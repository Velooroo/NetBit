# 🏗️ NetBit Architecture Documentation

## 📋 Overview

NetBit - это современная платформа для разработчиков, объединяющая Git-сервер, систему управления проектами и мессенджер в единую экосистему. Проект построен как монорепозиторий с четким разделением на платформы.

## 🎯 Core Concept

**Единая экосистема разработки = Git Server + Project Management + Real-time Chat**

## 🏗️ Monorepo Structure

```
netbit/
├── packages/                    # Основные пакеты приложения
│   ├── backend/                 # Rust backend (Git HTTP Server + API)
│   ├── web/                     # React веб-приложение
│   ├── mobile/                  # React Native мобильное приложение
│   └── shared/                  # Общие TypeScript типы и API клиент
├── docs/                        # Документация проекта
├── PROJECT_ROADMAP.md          # Дорожная карта развития
├── README.md                   # Основная документация
├── QUICKSTART.md              # Быстрый старт
├── DOCUMENTATION.md           # Полная документация
└── package.json               # Workspace конфигурация
```

## 🦀 Backend Architecture (Rust)

### Technology Stack
- **Rust 1.70+** - системный язык программирования для производительности и безопасности
- **Actix Web 4.x** - асинхронный веб-фреймворк с высокой производительностью
- **SQLite** - встроенная база данных для простоты развертывания
- **Serde** - сериализация/десериализация JSON
- **Git2** - библиотека для работы с Git репозиториями
- **Rusqlite** - SQLite драйвер для Rust

### Code Structure

```
packages/backend/src/
├── main.rs                      # 🚀 Точка входа, конфигурация HTTP сервера
├── core/                        # 🔧 Ядро системы - базовая функциональность
│   ├── mod.rs                   # Экспорт модулей ядра
│   ├── config.rs                # Конфигурация из env переменных
│   ├── database.rs              # SQLite подключение и миграции
│   ├── auth.rs                  # JWT аутентификация (TODO)
│   └── types.rs                 # Общие типы данных (API Response, Errors)
├── domain/                      # 📊 Доменные модели - бизнес-логика
│   ├── mod.rs                   # Экспорт доменных моделей
│   ├── user.rs                  # Модель пользователя
│   ├── project.rs               # Модель проекта
│   ├── repository.rs            # Модель Git репозитория
│   ├── chat.rs                  # Модель чата и сообщений
│   └── notification.rs          # Модель уведомлений
├── api/                         # 🌐 HTTP API обработчики
│   ├── mod.rs                   # Экспорт API модулей
│   ├── user.rs                  # Эндпоинты пользователей (auth, profile)
│   ├── project.rs               # Эндпоинты проектов (CRUD)
│   ├── git.rs                   # Git Smart HTTP Protocol
│   ├── chat.rs                  # Эндпоинты чатов (real-time)
│   ├── notification.rs          # Эндпоинты уведомлений
│   └── repo.rs                  # Устаревшие эндпоинты (TODO: удалить)
└── utils/                       # 🛠️ Утилиты и вспомогательные функции
    ├── mod.rs                   # Экспорт утилит
    ├── git.rs                   # Git операции (клонирование, push/pull)
    └── helpers.rs               # Общие вспомогательные функции
```

### Database Schema

```sql
-- Пользователи системы
users (id, username, password, email, created_at)

-- Проекты (могут содержать несколько репозиториев)
projects (id, name, owner_id, description, is_public, created_at)

-- Конфигурации проектов (JSON)
project_configs (id, project_id, config_json, updated_at)

-- Git репозитории
repositories (id, name, project_id, owner_id, description, is_public, created_at)

-- Чаты (direct, group, channel)
chats (id, name, chat_type, creator_id, created_at, updated_at)

-- Участники чатов
chat_participants (chat_id, user_id, role, joined_at, last_read_at)

-- Сообщения
messages (id, chat_id, sender_id, content, message_type, created_at, is_edited, edited_at)

-- Уведомления
notification (id, name, content, created_at)
```

### API Endpoints

#### Authentication
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация нового пользователя

#### User Management
- `GET /api/user/profile` - Получение профиля пользователя

#### Project Management
- `GET /api/projects` - Список проектов пользователя
- `GET /api/projects/public` - Публичные проекты
- `POST /api/projects/create` - Создание нового проекта
- `GET /api/projects/{user}/{project}` - Информация о проекте
- `GET /api/projects/{user}/{project}/config` - Конфигурация проекта
- `PUT /api/projects/{user}/{project}/config` - Обновление конфигурации

#### Chat System
- `GET /api/chats` - Список чатов пользователя
- `POST /api/chats` - Создание нового чата
- `GET /api/chats/{id}` - Информация о чате
- `GET /api/chats/{id}/messages` - Сообщения чата
- `POST /api/chats/{id}/messages` - Отправка сообщения

#### Git Smart HTTP Protocol
- `GET /git/{user}/{repo}/info/refs` - Git info refs
- `POST /git/{user}/{repo}/git-upload-pack` - Git fetch/pull
- `POST /git/{user}/{repo}/git-receive-pack` - Git push

## ⚛️ Frontend Architecture (React Web)

### Technology Stack
- **React 18** - современная UI библиотека с Concurrent Features
- **TypeScript** - типизированный JavaScript для надежности
- **Vite** - быстрый сборщик и dev сервер
- **Tailwind CSS** - utility-first CSS фреймворк
- **React Router** - клиентская маршрутизация
- **Axios** - HTTP клиент для API

### Structure
```
packages/web/src/
├── main.tsx                     # React приложение entry point
├── App.tsx                      # Главный компонент
├── index.css                    # Глобальные стили
├── components/                  # React компоненты
├── pages/                       # Страницы приложения
├── context/                     # React Context для state management
└── lib/                         # Библиотеки и утилиты
```

## 📱 Mobile Architecture (React Native)

### Technology Stack
- **React Native 0.76** - кроссплатформенная разработка
- **Expo SDK 52** - платформа для React Native разработки
- **Expo Router 4** - файловая система маршрутизации
- **TypeScript** - типизация для мобильного кода
- **React Native Reanimated** - анимации
- **Expo Symbols** - нативные иконки

### Structure
```
packages/mobile/
├── app/                         # Expo Router файловая маршрутизация
│   ├── (auth)/                  # Группа экранов аутентификации
│   ├── (protected)/             # Защищенные экраны
│   ├── _layout.tsx              # Корневой layout
│   └── index.tsx                # Главная страница
├── components/                  # React Native компоненты
│   ├── base/                    # Базовые компоненты
│   ├── ui/                      # UI компоненты
│   └── tabs/                    # Табы навигации
├── constants/                   # Константы (цвета, данные)
├── context/                     # React Context
├── hooks/                       # Custom React hooks
├── utils/                       # Утилиты
└── assets/                      # Статические ресурсы
```

## 🔗 Shared Package

### Purpose
Централизованные TypeScript типы и API клиент для переиспользования между веб и мобильными платформами.

### Structure
```
packages/shared/src/
├── index.ts                     # Главный экспорт
├── types/                       # TypeScript типы
│   └── index.ts                 # API типы, модели данных
└── api/                         # HTTP клиент
    ├── client.ts                # Базовый API клиент (Axios)
    └── services.ts              # Сервисы для различных API
```

### API Client Features
- Автоматическое добавление JWT токенов
- Обработка ошибок HTTP
- Типизированные запросы и ответы
- Поддержка пагинации
- Interceptors для auth и error handling

## 🔄 Data Flow

### Authentication Flow
1. Пользователь вводит credentials на клиенте
2. Клиент отправляет POST /api/auth/login
3. Backend валидирует и возвращает JWT токен
4. Клиент сохраняет токен и добавляет в заголовки
5. Последующие запросы автоматически включают токен

### Real-time Chat Flow
1. Установка WebSocket соединения
2. Клиент подписывается на чаты пользователя
3. При отправке сообщения:
   - POST /api/chats/{id}/messages
   - Broadcast через WebSocket всем участникам
4. Клиенты получают real-time обновления

### Git Operations Flow
1. Git клиент делает HTTP запрос к /git/{user}/{repo}
2. Backend проверяет права доступа
3. Выполняется соответствующая Git операция
4. Возвращается Git Smart HTTP Protocol ответ

## 🔐 Security Considerations

### Authentication & Authorization
- JWT токены для stateless аутентификации
- Bcrypt для хеширования паролей
- RBAC (Role-Based Access Control) для проектов
- CORS настройки для кроссплатформенности

### Input Validation
- Валидация на уровне API
- Защита от SQL инъекций через параметризованные запросы
- XSS защита через правильную сериализацию

### Git Security
- Проверка прав доступа к репозиториям
- Валидация Git операций
- Изоляция репозиториев по пользователям

## 📊 Performance Considerations

### Backend Optimizations
- Connection pooling для базы данных
- Асинхронная обработка запросов (Actix Web)
- Efficient SQL запросы с индексами
- Кэширование часто запрашиваемых данных

### Frontend Optimizations
- Code splitting с React.lazy()
- Мемоизация компонентов
- Виртуализация больших списков
- Оптимизация изображений

### Database Optimizations
- Индексы на часто запрашиваемых полях
- Пагинация для больших наборов данных
- Efficient schema design

## 🚀 Deployment Architecture

### Development
```
Developer Machine
├── Backend: cargo run (port 8000)
├── Web: vite dev (port 5173)
└── Mobile: expo start
```

### Production (Planned)
```
Production Server
├── Backend: Rust binary behind reverse proxy
├── Web: Static files served by nginx
├── Database: SQLite (или PostgreSQL для scale)
└── Mobile: Distributed via app stores
```

## 🔮 Future Architecture Plans

### Microservices Evolution
- Разделение на отдельные сервисы по доменам
- Message queue для асинхронной обработки
- Service discovery и load balancing

### Real-time Enhancements
- WebSocket clustering для горизонтального масштабирования
- Redis для shared state между инстансами
- Push notifications для мобильных приложений

### NetBit Daemon
- Системный сервис для background операций
- TUI интерфейс для разработчиков
- Интеграция с OS для умных уведомлений

---

**Last Updated:** December 2024  
**Architecture Version:** 1.0  
**Status:** Foundation Phase