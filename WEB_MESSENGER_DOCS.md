# 💬 NetBit Web Messenger - Документация

## Обзор

NetBit Web Messenger - это веб-клиент для системы обмена сообщениями между участниками команды разработчиков. Построен на React с TypeScript и использует современные паттерны разработки.

## 🏗️ Архитектура компонентов

### ChatPage.tsx - Основной интерфейс чата

**Назначение:** Главная страница мессенджера с интерфейсом чата в стиле современных мессенджеров.

**Основные возможности:**
- Список активных чатов с поиском
- Область сообщений с real-time обновлениями
- Поле ввода с поддержкой эмодзи и файлов
- Индикация онлайн статуса
- Счетчики непрочитанных сообщений

**Структура данных:**
```typescript
interface Message {
  id: number;
  content: string;
  sender: string;
  timestamp: Date;
  isOwn: boolean;
}

interface Chat {
  id: number;
  name: string;
  avatar: string;
  lastMessage: string;
  timestamp: string;
  unread: number;
  online: boolean;
}
```

**Тестовые данные:**
- 5 предустановленных чатов (личные и групповые)
- История сообщений для демонстрации
- Реалистичные имена и аватары

### ProfilePage.tsx - Профиль пользователя

**Назначение:** Страница профиля пользователя с информацией, проектами и активностью.

**Основные разделы:**
- **Информация о пользователе** - контакты, статус, дата регистрации
- **Статистика** - количество проектов, репозиториев, подписчиков
- **Табы контента:**
  - `Обзор` - последние проекты и активность
  - `Проекты` - все проекты пользователя с управлением
  - `Активность` - лента последних действий

**Структура данных:**
```typescript
interface UserProfile {
  id: number;
  name: string;
  username: string;
  email: string;
  phone: string;
  location: string;
  joinDate: string;
  avatar: string;
  status: string;
  bio: string;
  projects: number;
  repositories: number;
  followers: number;
}

interface Project {
  id: number;
  name: string;
  description: string;
  language: string;
  stars: number;
  lastUpdate: string;
}
```

## 🎨 UI/UX Дизайн

### Цветовая схема
- **Основной цвет:** `#3B82F6` (Blue 500)
- **Фон:** `#F9FAFB` (Gray 50)
- **Карточки:** `#FFFFFF` (White)
- **Текст:** `#111827` (Gray 900), `#6B7280` (Gray 500)
- **Активные элементы:** `#EFF6FF` (Blue 50)

### Типографика
- **Заголовки:** `font-bold`, размеры от `text-lg` до `text-2xl`
- **Основной текст:** `text-sm` и `text-base`
- **Вторичный текст:** `text-gray-500`, `text-xs`

### Компоненты
- **Кнопки:** Rounded corners (`rounded-lg`), hover эффекты
- **Инпуты:** Border focus с изменением цвета
- **Карточки:** Subtle shadows, hover эффекты
- **Аватары:** Круглые с online индикаторами

## 🔧 Техническая реализация

### Зависимости
```json
{
  "lucide-react": "^0.x.x",  // Иконки
  "react-router-dom": "^6.x.x", // Маршрутизация
  "tailwindcss": "^3.x.x"    // Стили
}
```

### Маршрутизация
```typescript
// App.tsx - обновленная маршрутизация
<Routes>
  <Route path="/" element={user ? <HomePage /> : <Navigate to="/login" />} />
  <Route path="/chat" element={user ? <ChatPage /> : <Navigate to="/login" />} />
  <Route path="/profile" element={user ? <ProfilePage /> : <Navigate to="/login" />} />
  // ... остальные маршруты
</Routes>
```

### Навигация
```typescript
// Header.tsx - обновленная навигация
const navigation = [
  { path: '/', icon: GitBranch, label: 'Репозитории' },
  { path: '/chat', icon: MessageCircle, label: 'Чаты' },
  { path: '/profile', icon: User, label: 'Профиль' }
];
```

## 📱 Responsive дизайн

### Breakpoints
- **Mobile:** `< 768px` - стек layout, скрытие сайдбара
- **Tablet:** `768px - 1024px` - адаптация sidebar
- **Desktop:** `> 1024px` - полный интерфейс

### Адаптивные элементы
- **ChatPage:** Sidebar скрывается на мобильных
- **ProfilePage:** Grid переходит в column layout
- **Header:** Сворачивание навигации в hamburger menu

## 🚀 Запуск и разработка

### Локальная разработка
```bash
cd packages/web
npm install
npm run dev
```

### Сборка
```bash
npm run build
npm run preview
```

### Тестирование
```bash
npm run test
npm run test:coverage
```

## 🔄 Интеграция с Backend

### API эндпоинты (планируемые)
```typescript
// Chat API
GET /api/chats - получение списка чатов
GET /api/chats/:id/messages - сообщения чата
POST /api/chats/:id/messages - отправка сообщения
WebSocket /ws/chat - real-time обновления

// Profile API  
GET /api/users/:id - профиль пользователя
PUT /api/users/:id - обновление профиля
GET /api/users/:id/projects - проекты пользователя
GET /api/users/:id/activity - активность пользователя
```

### WebSocket интеграция
```typescript
// Планируемая WebSocket интеграция
const ws = new WebSocket('ws://localhost:8000/ws/chat');

ws.onmessage = (event) => {
  const message = JSON.parse(event.data);
  // Обновление состояния чата
  setMessages(prev => [...prev, message]);
};
```

## 🎯 Следующие шаги

### Краткосрочные задачи
1. **Real-time функциональность** - WebSocket интеграция
2. **Файловые вложения** - поддержка загрузки файлов
3. **Эмодзи** - интеграция emoji picker
4. **Уведомления** - browser notifications

### Долгосрочные планы
1. **Темы оформления** - светлая/темная тема
2. **Голосовые сообщения** - запись и воспроизведение
3. **Видеозвонки** - интеграция WebRTC
4. **Поиск по сообщениям** - полнотекстовый поиск

## 📋 Тестирование

### Тестовые сценарии
1. **Навигация между чатами** - переключение, загрузка сообщений
2. **Отправка сообщений** - ввод, отправка, отображение
3. **Поиск чатов** - фильтрация по имени
4. **Профиль пользователя** - просмотр, переключение табов
5. **Responsive дизайн** - тестирование на разных экранах

### Интеграционные тесты
1. **API интеграция** - моки для backend API
2. **WebSocket тесты** - симуляция real-time событий
3. **E2E тесты** - полные пользовательские сценарии

## 🔐 Безопасность

### Меры безопасности
1. **XSS защита** - экранирование пользовательского контента
2. **CSRF токены** - защита форм
3. **Input валидация** - проверка на клиенте и сервере
4. **Content Security Policy** - ограничение загружаемых ресурсов

### Аутентификация
- JWT токены в localStorage
- Автоматическое обновление токенов
- Logout при истечении сессии

---

**Статус:** ✅ Готово к тестированию  
**Версия:** 1.0.0  
**Последнее обновление:** Декабрь 2024