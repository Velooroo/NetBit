pub struct ScheduleModule;

impl ScheduleModule {
    pub fn new() -> Self {
        ScheduleModule
    }

    pub async fn check_tasks(&self) {
        // Заглушка для таймеров / уведомлений
    }
}
